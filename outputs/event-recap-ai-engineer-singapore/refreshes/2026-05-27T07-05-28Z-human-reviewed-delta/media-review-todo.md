# Media review todo

The candidate now suppresses media from comments, replies, and non-root context posts in the public media wall.

Known follow-up before promotion, if we want image-level precision:

- `linkedin_1p0l8ex` / `7463471918340390912` / Sara Hooker: keep the post as relevant, but review the 7-image album. The post text is directly event-relevant; the album mixes broader Singapore trip/community photos.
- `linkedin_16w2sr7` / `7462675979296141312` / William Liu: keep only as context if we keep Build Night / side-event texture. Its media should not be promoted as primary event media unless the event tie is explicitly approved.
- `x:2056188953695547872` / Sherry Jiang: keep the post as event texture because it explicitly references `@aiDotEngineer` Singapore and the expo, but review/suppress the child-centered image before promotion unless we deliberately want family-at-expo texture in the media wall.
- Add per-media review metadata later, e.g. `media[].relevance = primary_event | context | off_topic`, instead of accepting or dropping an entire parent post's media.

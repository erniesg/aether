# aether local SAM3 + CLIP server deps.
#
# Tested on Mac (Apple Silicon, MPS) and Linux (CUDA). Pin major versions to
# match the Modal app's lockfile so behaviour stays parity-equivalent.

# Web server
fastapi>=0.115,<1
uvicorn[standard]>=0.32,<1
pydantic>=2.9,<3

# CLIP stack
torch>=2.5,<3            # MPS support for SAM3 lands in 2.5+; pin lower bound
torchvision>=0.20,<1
open-clip-torch>=2.26,<3
hdbscan>=0.8.40,<1
umap-learn>=0.5.7,<1
scikit-learn>=1.5,<2
numpy>=1.26,<2
pillow>=10.4,<12

# SAM3 — Mac-friendly fork that swaps the Triton EDT kernel for OpenCV.
# The official facebook/sam3 hard-imports `triton`, which is Linux/CUDA only
# and won't install on macOS. The fork is API-compatible (same Sam3Processor
# + build_sam3_image_model surface). For Linux+CUDA dev, swap to the upstream
# facebook/sam3 commit pinned in modal/sam3_app.py.
git+https://github.com/MaximeLglr/sam3-apple-silicon.git

# OpenCV — used by the Mac fork's EDT replacement.
opencv-python>=4.10,<5
pycocotools>=2.0.11
psutil>=7

# Misc
huggingface_hub>=0.25,<1
timm>=1.0.17
ftfy>=6
regex
iopath>=0.1.10
typing_extensions
einops

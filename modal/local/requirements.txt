# aether local SAM3 + CLIP server deps.
#
# Tested on Mac (Apple Silicon, MPS) and Linux (CUDA). Pin major versions to
# match the Modal app's lockfile so behaviour stays parity-equivalent.

# Web server
fastapi>=0.115,<1
uvicorn[standard]>=0.32,<1
pydantic>=2.9,<3

# CLIP stack
torch>=2.5,<3            # MPS support for SAM3 lands in 2.5+; pin lower bound
torchvision>=0.20,<1
open-clip-torch>=2.26,<3
hdbscan>=0.8.40,<1
umap-learn>=0.5.7,<1
scikit-learn>=1.5,<2
numpy>=1.26,<2
pillow>=10.4,<12

# SAM3 (install from the public commit baked into modal/sam3_app.py)
# Pin to the same revision so weights / processor signatures match.
git+https://github.com/facebookresearch/sam3.git@2e0009e23f0ad0fbcbd0488df893d30d5c8c2565

# Misc
huggingface_hub>=0.25,<1
timm>=1.0.17
ftfy==6.1.1
regex
iopath>=0.1.10
typing_extensions
einops
